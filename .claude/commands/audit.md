Your goal is to update any vulnerable dependencies.

Do the following:

Run `pnpm audit` to find vulnerable installed packages in this project
Run `pnpm audit fix` to apply updates
Run tests and verify the updates didn't break anything

If any tests fail, revert the changes and investigate the failures before proceeding.